import React from 'react';
import PropTypes from 'prop-types';

function ItemAward ({title, desc}) {
  return (
    <div></div>
  )
}

ItemAward.propTypes = {
  title: PropTypes.string,
  desc: PropTypes.string,
};

export default ItemAward